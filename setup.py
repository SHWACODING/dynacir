from setuptools import setup, find_packages

setup(
    name='dynacir',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.6',
    # Add more parameters as needed
)
